import Engine from "./engine";
import { Base, EngineData, ServerStatus } from "../type";

// const engine = new Engine(Base.baseUrl);

// export async function startEngine(carId: string): Promise<EngineData> {
//   const engineData: EngineData = await engine.start(carId);
//   console.log("startEngine() engine data = ", engineData);
//   return engineData;
// }
// export async function goCar(carId: string): Promise<ServerStatus> {
//   const serverStatus: ServerStatus = await engine.go(carId);
//   return serverStatus;
// }
// export function abortConnect(): void {
//   engine.abortConnect();
// }
// export function newConnect(): void {
//   engine.newConnect();
// }
